
var spinner = $('#user-tool-spinner');
spinner.before('<%= escape_javascript( display_user_tools(@user_tools).html_safe) %>');

<% if params[:page].to_i > @user_tools.total_pages%>
spinner.removeClass('spinner');
spinner.css('height','0');
<% end %>
