
var notiArea = $('.notis-area');

$('#notification-div').removeClass('spinner');
$('.noti-lower-area').html('<ul id="noti-ul">'+

        '<%= escape_javascript( display_notifications(@current_user_notifications).html_safe)%>' +'</ul>');

notiArea.mCustomScrollbar({
    axis: "y",
    scrollbarPosition: "inside",
    autoHideScrollbar: true,
    theme: "light-thin",
    advanced:{ updateOnSelectorChange: "noti-lower-area" }


});

notiArea.find('.mCSB_container').css('margin-right', '0px');

console.log('test');

